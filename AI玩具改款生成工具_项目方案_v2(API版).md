# AI 玩具创意生成工具 —— 项目方案 v2（API 云端版）

> 基于原始本地部署方案迭代而来。核心变化：由本地私有化部署改为调用云端 API（视觉理解、LLM、生图），并将"轮廓提取 + 精修 prompt"两路并行汇入生图模型，解决结构保真度问题。

---

## 一、项目定位

面向 B 端玩具工厂 / 产品设计师，输入原型玩具的**图片或短视频**，系统自动完成：

1. **原型拆解**：识别外观、机械结构、电子硬件、原生玩法、卖点、痛点
2. **同源改款方案 ×1**：内核不变，仅换外壳造型 + 微调固件逻辑
3. **同内核换形象衍生方案 ×5**：5 个不同 IP / 生物形象，硬件机械完全复用
4. **每套方案配套效果图**：由生图模型基于原型结构生成，而非凭空想象

产出物：产品名、外观描述、改动清单、新增玩法、卖点、工艺风险提示、效果图，支持导出 PDF/Markdown。

**部署方式**：改为调用云端 API，不再要求本地部署 GPU 服务器，开发和验证周期大幅缩短；上线给工厂使用前需单独评估数据合规问题（见第六章）。

---

## 二、总体架构

```
                         ┌─────────────────────────┐
                         │   原始图片 / 视频关键帧    │
                         └────────────┬─────────────┘
                                      │
                   ┌──────────────────┴──────────────────┐
                   ▼                                      ▼
     ┌─────────────────────────┐          ┌──────────────────────────────┐
     │   模块A：视觉理解 API      │          │  模块B：结构参考图（可选，先跳过）  │
     │  输出结构化JSON拆解报告    │          │  Seedream原生支持图生图+涂鸦控制  │
     └────────────┬─────────────┘          │  一般无需单独出edge/depth图      │
                   │                        └───────────────┬────────────────┘
                   ▼                                        │
     ┌─────────────────────────┐                            │
     │   模块C：LLM方案生成      │                            │
     │  文案 + 精修生图prompt    │                            │
     └────────────┬─────────────┘                            │
                   └──────────────────┬───────────────────────┘
                                      ▼
                         ┌─────────────────────────┐
                         │  模块D：生图（Seedream）  │
                         │  原图作参考 + 精修prompt   │
                         └────────────┬─────────────┘
                                      ▼
                         ┌─────────────────────────┐
                         │  模块E：方案汇总与导出     │
                         │  报告 + 效果图 + PDF/MD   │
                         └────────────┬─────────────┘
                                      ▼
                         ┌─────────────────────────┐
                         │  模块F：二次迭代交互       │
                         │  选中方案继续下达修改指令   │
                         └─────────────────────────┘
```

**关键设计原则**：模块A（视觉理解）和模块D的"原图参考"是同一张图的两种用途，天然可以并行准备；模块C的文字 prompt 必须先于模块D的生图调用完成，二者是串联关系，不是"各自独立生成再合并"。

---

## 三、模块详细设计

### 模块 A：媒体输入与预处理

**职责**：接收图片或短视频，统一转成送入视觉模型的图片帧。

- 输入类型：`jpg/png/webp` 图片，或 `mp4/mov` 本地短视频
- 视频处理：用 `opencv-python` 抽取 3–5 帧关键帧（按场景变化/清晰度筛选，丢弃模糊帧和重复帧）
- 输出：1 张主图（用于生图参考）+ 若干补充帧（用于视觉模型多角度理解）

```python
import cv2

def extract_keyframes(video_path, max_frames=5, sharpness_threshold=100):
    cap = cv2.VideoCapture(video_path)
    frames = []
    while cap.isOpened():
        ret, frame = cap.read()
        if not ret:
            break
        # 用拉普拉斯方差判断清晰度，过滤模糊帧
        sharpness = cv2.Laplacian(cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY), cv2.CV_64F).var()
        if sharpness > sharpness_threshold:
            frames.append(frame)
    cap.release()
    # 简单等间隔采样，按需替换为场景检测算法
    step = max(1, len(frames) // max_frames)
    return frames[::step][:max_frames]
```

---

### 模块 B：视觉理解（结构化拆解）

**职责**：调用多模态视觉 API，把玩具图片解析成结构化 JSON。

**API 选型建议**（任选其一，中文场景优先前二者）：

| 供应商 | 模型 | 备注 |
|---|---|---|
| 阿里云百炼 | 通义千问 VL-Max | 中文理解好，结构化输出稳定 |
| 火山引擎方舟 | 豆包视觉理解模型 | 与后续 Seedream 同平台，便于统一鉴权计费 |
| Anthropic / OpenAI | Claude / GPT-4V 系列 | 多模态能力强，国际化场景可选 |

**Prompt 模板**（与原方案一致，保留强约束）：

```
你是玩具行业产品分析师，分析图中的玩具，严格输出JSON格式，不要多余文字。字段：
{
  "外观": "",
  "机械结构": "",
  "电子硬件": "",
  "原生玩法": "",
  "现有卖点": "",
  "产品痛点": ""
}
约束：只基于图片可见内容描述，看不清楚的部分标注"图片不可见"，不要幻觉编造。
```

**输出示例**：见原方案文档中的机器小狗 JSON 样例，结构保持不变。

**关键风险控制**：视觉模型容易对看不清的内部结构"脑补"，Prompt 中必须显式要求标注不可见部分，避免生成方案里出现虚构的硬件细节。

---

### 模块 C：LLM 方案生成（文案 + 精修 prompt）

**职责**：把模块 B 的 JSON + 业务硬性约束喂给 LLM，产出 1 套同源改款 + 5 套衍生方案的完整文案，并为每套方案生成"给生图模型用"的精修 prompt。

**业务规则 Prompt（系统层，必须放在 system prompt 里反复强调）**：

```
【硬性工厂规则】
1. 所有衍生产品，电路板、电机、传感器、屏幕全部沿用原型，不允许新增任何硬件。
2. 仅可修改外壳造型，固件程序只做小幅逻辑调整（如新增表情/动作序列），不得改动硬件。
3. 只新开外壳模具，内部结构尺寸必须与原有硬件完全兼容。
4. 每套方案的风险提示中，必须包含"外壳内部装配腔体需与原有限位、卡扣尺寸对齐"这一条。
5. 若原型玩具无固件源代码，需在风险提示中标注"固件微调依赖原厂源码，否则无法实现"。

【输出要求】
针对每套方案（1套同源改款 + 5套衍生），输出以下字段：
- 产品名称
- 外观描述
- 改动点清单
- 新增玩法
- 产品卖点
- 工艺风险提示
- 生图prompt（英文，供文生图/图生图模型使用，需体现：整体轮廓与原型一致、外观改动细节、材质配色、摄影棚产品图风格）
```

**输出结构建议用 JSON**，便于后续程序化处理和批量调用生图 API：

```json
{
  "restyle": { "name": "", "appearance": "", "changes": [], "gameplay": [], "selling_points": [], "risks": [], "image_prompt": "" },
  "derivatives": [
    { "name": "", "appearance": "", "changes": [], "gameplay": [], "selling_points": [], "risks": [], "image_prompt": "" }
  ]
}
```

---

### 模块 D：生图（Seedream 图生图）

**职责**：用原图作为结构参考 + 模块 C 生成的精修 prompt，生成每套方案的效果图。

**为什么选择图生图而非纯文生图**：文字 prompt 难以精确传达"外壳内部装配尺寸必须兼容原硬件"这类约束，图生图以原图为参考锚点，能天然保留整体轮廓比例，再靠 prompt 控制局部改动（耳朵形状、材质、配色等）。

**为什么可以省掉单独的 ControlNet/轮廓提取步骤**：Seedream 4.0 及以上版本已原生支持识别草图/涂鸦/辅助线做结构控制，不需要额外跑 Canny 边缘检测再接 ControlNet 模型，直接把原图作为参考图传入图生图接口即可。

**关键参数**：

| 参数 | 作用 | 建议 |
|---|---|---|
| `image` | 参考图（原型玩具照片） | 必填 |
| `prompt` | 模块C生成的精修prompt | 必填 |
| `strength` / 图生图强度 | 控制"贴近原图程度" | 建议先用 0.5–0.7 区间做 A/B 测试，找到"保轮廓但换得动物形象"的平衡点，5个衍生方案可能需要不同强度 |
| `size` | 输出分辨率 | 产品效果图建议 2K |
| `sequential` / 组图模式 | 一次生成多张变体供选择 | 每套方案可生成 2–3 张备选，人工挑选 |

**调用示例（伪代码，参照火山方舟图片生成 API）**：

```python
import requests

def generate_image(api_key, image_url, prompt, strength=0.6, size="2K"):
    resp = requests.post(
        "https://ark.cn-beijing.volces.com/api/v3/images/generations",
        headers={"Authorization": f"Bearer {api_key}"},
        json={
            "model": "doubao-seedream-4-5",
            "prompt": prompt,
            "image": image_url,
            "strength": strength,
            "size": size,
        },
    )
    return resp.json()
```

> 注：具体字段名以火山方舟官方最新 API 文档为准，调用前务必核对（文档更新频繁）。

---

### 模块 E：方案汇总与导出

**职责**：把模块C的文案 + 模块D的效果图组装成结构化报告，支持导出。

- 页面展示：1套同源改款 + 5套衍生方案，每套包含文案区 + 效果图区
- 导出格式：Markdown（默认，轻量，方便传给结构工程师）/ PDF（对外汇报用）
- 建议每套方案单独存一条记录（如 `plan_id`），方便后续迭代指令定位到具体方案

---

### 模块 F：二次迭代交互

**职责**：用户选中某一方案后继续下达指令，如"把这个恐龙改成黑色机甲风格，调整玩法"。

**实现方式**：
1. 前端记录用户当前选中的 `plan_id` 及其原始 JSON（文案+prompt）
2. 用户指令 + 该方案原始 JSON 一起重新喂给模块C（LLM），要求"仅修改用户指定的部分，其余字段保留"
3. 若涉及外观改动，重新生成 prompt 后再次调用模块D生图；若只涉及玩法/文案调整，跳过生图，直接更新文案

---

## 四、技术栈汇总

| 层 | 技术 |
|---|---|
| 后端 | Python + FastAPI |
| 前端 | Gradio（内部工具，快速验证用） |
| 视频处理 | opencv-python |
| 视觉理解 | 通义千问VL-Max / 豆包视觉理解 API（二选一或都接，做效果对比） |
| 方案生成 | 同平台LLM（如豆包大模型）或独立调用 Qwen/GLM 系列 API |
| 生图 | 火山引擎方舟 Seedream（doubao-seedream 系列），图生图模式 |
| 导出 | Markdown 直出 / weasyprint 或 pandoc 转 PDF |

---

## 五、开发路线图

**V1（当前目标，验证流程闭环）**
- 打通图片/视频输入 → 视觉理解 → LLM方案生成 → Seedream生图 → 报告导出 全流程
- 先用图片测试，视频抽帧作为次优先级
- 生图先用单一 strength 参数跑通，暂不做多参数 A/B

**V1.5（效果调优）**
- 针对"衍生形象差异大 vs 结构需保留"这个矛盾，批量测试 strength 参数，为不同衍生方案设定不同默认值
- 引入组图模式，每套方案出 2–3 张备选供人工挑图

**V2**
- 支持输入成本档位、目标市场，约束创意生成方向
- 增加简单 3D 建模提示词输出，辅助结构工程师
- 视频抽帧改用场景检测算法替代等间隔采样，提高关键帧质量

---

## 六、数据隐私与合规（务必在正式上线前过一遍）

- 原型玩具照片/视频属于工厂未公开的商业信息，调用第三方 API 前需确认：
  1. 服务商是否承诺不将上传数据用于模型训练（一般需要企业级协议，非默认条款）
  2. 是否有 IP 侵权风险（若原型本身是授权卡通形象，上传生成"仿制"图可能被内容审核拦截，也存在法律风险）
- 建议路径：先用非涉密的测试样机跑通全流程验证效果 → 效果达标后，与视觉/生图 API 供应商签订企业协议，明确数据不留存/不训练条款 → 或评估是否需要切回本地私有化部署处理真正涉密的原型

---

## 七、参考的开源实现（非完整方案，作为模块级参考）

| 项目 | 可借鉴点 |
|---|---|
| `ComfyUI-Seedream-API`（GitHub: JiangAogo/ComfyUI-Seedream-API） | Seedream 图生图 API 调用参数封装，可直接参考其调用代码结构 |
| AI Interior Redesign（SAM + SD inpainting + ControlNet） | "保留几何结构、替换材质/外观"的实现思路，与本项目"保内核换外壳"逻辑相似，可参考其分割+控制+重绘流程设计 |
| Hunyuan3D Studio 论文中的 Image Stylization 模块 | 用"改成XX风格，保持内容一致"的指令式 prompt 做风格迁移，思路可直接套用到衍生形象的 prompt 设计中 |
| `gradio-app/gradio` 官方仓库 | 内部工具前端搭建的标准示例 |

---

## 八、主要风险清单

| 风险 | 影响 | 应对 |
|---|---|---|
| 视觉模型幻觉，脑补看不见的内部结构 | 方案文案出现虚构硬件信息 | Prompt 强制标注"不可见"，人工复核关键方案 |
| 生图无法精确保留装配结构 | 效果图好看但结构工程师无法直接参考 | 图生图+调节strength，效果图仅作创意参考，最终结构仍需工程师用CAD复核 |
| 固件无源码，程序微调无法落地 | 承诺的新增玩法无法实现 | 方案生成阶段强制标注该风险 |
| 第三方API数据合规风险 | 涉密原型图外泄 | 见第六章，正式上线前单独评估 |
| API 费用与调用速率限制 | 批量生成5套方案+多张备选图成本上升 | 先用低分辨率/单备选跑通流程，验证后再放开分辨率和备选数量 |
